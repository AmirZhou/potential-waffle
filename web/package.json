{
  "dependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start:db": "json-server --watch db.json",
    "start:parcel": "npx parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
